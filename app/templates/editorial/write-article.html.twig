{% extends 'base.html.twig' %}

{% block title %}Write Article{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1>Write article</h1>
        </div>
        {{ form_errors(writeArticleForm) }}
        <div class="card-body">
            {{ form_start(writeArticleForm) }}
            {{ form_row(writeArticleForm.title) }}
            {{ form_row(writeArticleForm.content) }}
        </div>
        <div class="card-footer">
            <div class="row">
                <button type="submit" class="btn btn-primary">Save</button>
            </div>
        </div>
        {{ form_end(writeArticleForm) }}
    </div>
{% endblock %}

{% block javascripts %}
{% endblock %}