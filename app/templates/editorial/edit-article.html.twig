{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1>Edit article</h1>
        </div>
        {{ form_errors(editArticleForm) }}
        <div class="card-body">
            {{ form_start(editArticleForm) }}
            {{ form_row(editArticleForm.title) }}
            {{ form_row(editArticleForm.content) }}
        </div>
        <div class="card-footer">
            <div class="row">
                <button type="submit" class="btn btn-primary">Save</button>
            </div>
        </div>
        {{ form_end(editArticleForm) }}
    </div>
{% endblock %}

{% block javascripts %}
{% endblock %}